[build]
  publish = "dist"
  command = "make ci-setup ci"
  functions = "assets/src/netlify_functions"

[build.environment]
  # node-releases (via @babel/preset-env -> browserslist) requires Node >= 18.
  NODE_VERSION = "20"

[functions]
  node_bundler = "esbuild"
